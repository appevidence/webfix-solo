"""``app`` package — core capture / hashing / signing / verify modules.

These are placeholders for code being ported from Repo A
(``appevidence/evidence-capture-app``) with the web layer stripped out.
See ``docs/Current-State.md`` for the port checklist.
"""
